[project]
name = "backend"
version = "0.1.0"
description = "This is the backend for my exchange-finder website"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.135.2",
]
